{
	"name": "horde-defense-ecspresso",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "bun ./index.html",
		"build": "rm -rf dist && bun build ./index.html --outdir=dist --public-path=/project-mifune/ --minify --sourcemap=linked",
		"check:types": "tsc --noEmit",
		"check": "bun run check:types"
	},
	"dependencies": {
		"ecspresso": "^0.14.10",
		"three": "^0.184.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/three": "^0.184.0",
		"typescript": "^6.0.3"
	}
}
